#Retrieve the content based on taxonomy 
      $route = new CustomRestRoute("category"); 
      localhost/category/name of category will retrieve content and limit is 10(possible to customize the limit);

#it owuld be more functional if pagination machanism would add

